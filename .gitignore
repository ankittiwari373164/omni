# Dependencies — never commit, reinstalled via npm install
node_modules/

# Generated videos & temp files — large, machine-specific
outputs/
uploads/
logs/

# Local assets (synced via Supabase Storage, not git)
assets/

# Environment & secrets
.env
.env.local

# OS / editor junk
.DS_Store
Thumbs.db
*.log
profiles/
